<%= form_for @mobile, :html => {:multipart => true} do |f| %>
  <% if @mobile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mobile.errors.count, "error") %> prohibited this mobile from being saved:</h2>

      <ul>
      <% @mobile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :release %><br>
    <%= f.date_select :release %>
  </div>
  <div class="field">
    <%= f.label :Recommended %><br>
    <%= f.check_box :Recommended %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
